#ifndef MAIN_H
#define MAIN_H

#include <stdlib.h>
#include <stdio.h>
int _putchar(char c);
int checkNumber(char *s);
int getLength(char *s);
int largestCoin(int p[], int moneyRemainder, int len);

#endif /* MAIN_H */
